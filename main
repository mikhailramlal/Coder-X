-- Coder X 

local function MSG(text)
    local msg = Instance.new("Message"game:GetService("Workspace"))
    msg.Text = text
    task.wait(5)
    msg:Destroy()
    return 
end

MSG("Coming Soon! :)")
